package com.romulus.mobile.downloads.config

internal object DownloadLimits {
    const val MIN_CONCURRENCY = 1
    const val MAX_CONCURRENCY = 5
    const val DEFAULT_CONCURRENCY = MIN_CONCURRENCY
}
